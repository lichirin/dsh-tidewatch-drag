# dsh-tidewatch

**DeepSeek 峰谷时刻悬浮徽章**：一张悬浮在输入框旁的潮汐卡，告诉你此刻是峰期还是谷期、距下一阶段还有多久、本次会话花了多少钱。

- 状态点：**峰期橙红 / 谷期蓝绿**，一眼可辨
- 折叠态：`● 峰期 距谷期 03:33 · ¥0.12`
- 展开态（点击展开）：官方峰谷时段表（北京时间）、当前模型与档位价格、在售模型价格对照、本次会话 Token 明细、分模型花费、币种切换
- **展开面板可拖动 / 可缩放 / 自动选方向**：面板按徽章位置与剩余空间自动决定向上还是向下弹出（输入框被顶到视口顶端时自动翻到下方，不会跑到屏幕外）；拖动头部移动、拖动抓手改尺寸，尺寸与位置写入 localStorage、刷新后保留，双击头部或点「复位面板」恢复默认
- 计费：**按模型分档**（官方现售 `deepseek-flash` 与 `deepseek-v4-pro` 两档价格），按每次调用**实际发生时刻**的档位计价，缓存命中/未命中分开，峰时 = 谷时 × 2
- 币种：默认人民币显示（固定汇率 6.82，与官方人民币标价逐位一致），展开面板可一键切换美元（4 位小数）
- 跟随 GUI 亮/暗主题（`--dsw-*` 变量）

## 峰谷时段（官方依据）

DeepSeek 官方自 2026-08-17 起实施峰谷分时定价：

| 时段（UTC） | 北京时间 | 档位 |
|---|---|---|
| 01:00 – 04:00 | 09:00 – 12:00 | 峰 |
| 04:00 – 06:00 | 12:00 – 14:00 | 谷 |
| 06:00 – 10:00 | 14:00 – 18:00 | 峰 |
| 10:00 – 次日 01:00 | 18:00 – 次日 09:00 | 谷 |

谷期价格为峰期的一半。徽章按 UTC 窗口判定当前档位（官方口径），时段表展示按北京时间。

**周末规则**：官方口径为**高峰时段仅周一至周五**（北京时间工作日 09:00–12:00、14:00–18:00），因此周六/周日（UTC 自然日）全天按谷期计价、无峰谷切换；下一阶段切换点为下周一首次进入峰时。

## 计费口径

- **按模型名分档**（官方 2026-09-10 更新日志 + 定价页脚注）：

  | 模型（API 模型名） | 说明 |
  |---|---|
  | `deepseek-flash` | DeepSeek-V4.1-Flash，在售 |
  | `deepseek-v4-pro` | DeepSeek-V4-Pro-0813；官方确认 **2026-09-14 之后继续提供 API 服务，计费方式不变** |
  | `deepseek-v4-flash`、`deepseek-v4-flash-vision-exp` | 旧模型已下线，官方**暂时**路由到 V4.1 Flash 并按 **Flash 价格**计费 |
  | 其他未收录名 | 按 Flash 价估算，插件在宿主日志里提示一次 |

- 官方标价（人民币 / 1M tokens）：

  | 模型 | Token 桶 | 谷时 | 峰时（×2） |
  |---|---|---|---|
  | `deepseek-flash` | 输入 · 缓存命中 | ¥0.02 | ¥0.04 |
  | `deepseek-flash` | 输入 · 缓存未命中 | ¥1 | ¥2 |
  | `deepseek-flash` | 输出 | ¥4 | ¥8 |
  | `deepseek-v4-pro` | 输入 · 缓存命中 | ¥0.15 | ¥0.30 |
  | `deepseek-v4-pro` | 输入 · 缓存未命中 | ¥4.5 | ¥9 |
  | `deepseek-v4-pro` | 输出 | ¥13.5 | ¥27 |

- 账本金额以**美元**存储（= 官方人民币标价 ÷ 6.82），成本 = 输入未命中 × cacheMiss + 输出 × output + (缓存读 + 缓存写) × cacheHit。DeepSeek 接口不单列缓存写入、也不单列推理计费（推理 token 已计入输出），故这两项不额外收费
- 价格表以**官方人民币标价**为权威源（`lib/pricing.js` 的 `rates.cny`），美元账本由它换算，改价时只需改人民币三档
- 峰谷时代之前（2026-08-16 16:00 UTC）的调用按当时的基础价计费（历史正确性）
- **调价不改写历史**：官方调价或模型分档变化后不提升投影 `stateVersion`，已缓存的会话保留当时算出的金额（老会话当时确实花了那么多钱）；从未计过费的会话首次打开时按**当前**价与**当前**模型档位计算
- 每次调用的费用按**事件发生时刻**的档位计算，跨峰谷切换不漂移；同一会话中途切换模型时，各次调用分别按**各自模型**单价计算
- 显示时按固定汇率 6.82 换算人民币（默认，数值与官方人民币标价一致）或直接显示美元（4 位小数）；分模型花费按各模型自身单价、以**当前**峰谷档位重算，与徽章合计同档位、不混口径

## 汇率基准

插件账本以美元计价，人民币展示 = 美元 × **固定汇率 6.82**。6.82 不是外汇市场牌价，而是
**官方定价页人民币标价与美元标价的隐含汇率**：官方当时对 1M tokens 输出同时给出 ¥4.50 与 $0.66，
4.50 ÷ 0.66 ≈ 6.8181…，取 6.82。

定价改版后它仍是插件唯一的换算基准：价格表里的美元值由「官方人民币标价 ÷ 6.82」反推，展示时再乘回
6.82，因此**人民币显示值与官方人民币标价逐位一致**（美元视图为换算值，官方美元标价四舍五入后可能有末位差异，
对账请用人民币视图）。若官方人民币标价与美元标价之比发生变化，需同时修改
`lib/pricing.js` 的 `CNY_PER_USD` 与 `lib/client.js` 的 `FIXED_FX`（`test/verify.mjs` 会校验二者一致）。

## 安装

> 需求：Node.js ≥ 20 + DeepSeek Harness（带 `dsh plugin` 命令的版本）。

```sh
# npm 包安装（发布后可用）
dsh plugin --profile web add dsh-tidewatch

# 或本地目录（开发调试）
dsh plugin --profile web add link:./dsh-tidewatch
```

安装后重启 `dsh web` 生效。

## 使用

- 徽章悬浮在输入框右侧、与输入框垂直居中对齐；窄窗口放不下时自动移到输入框上方，不遮挡输入框与官方统计栏
- 点击徽章展开/收起详情面板：时段表、当前模型与档位价格、在售模型价格对照（标出「使用中」）、Token 明细、分模型花费、币种切换（¥ / $）
- 点击「本次会话费用」行可展开**分模型花费明细**（如 `deepseek-flash` 与 `deepseek-v4-pro` 各自的 token 与花费分别列出，合计 = 分模型之和）
- 币种选择即时生效并记住；人民币 2 位小数，美元 4 位小数
- 会话中途切换模型时，徽章「当前模型」与价格行会实时跟随最近一次请求头更新

### 面板弹出方向与自适应定位

面板用 **fixed 定位**，坐标由 `placePanel()` 按「徽章实时位置 + 视口可用空间」算出，因此**永远不会跑到可视范围外**：

| 情形 | 面板行为 |
|---|---|
| 徽章上方有空间（默认） | 向上弹出，底边贴在徽章上方 10px |
| 徽章上方放不下（长会话向下滚动后输入框被顶到视口顶端） | **自动翻到徽章下方**，顶边贴徽章下方 10px |
| 徽章右侧空间不足 | 翻到徽章左侧；两侧都不足时贴视口内侧 |
| 视口变矮 / 面板内容变高 | `maxHeight` 被压到可用空间，超出部分面板内滚动 |
| 自定义尺寸 + 位移后窗口缩放 | 位移是相对「自动位置」的增量，缩放后会重新自适应，不会把面板甩出屏幕 |

- 方向只用固定门槛判定（上方可用空间 < 面板最低高度 180px 才翻转），因此不会出现「翻上去→装满→又翻下来」的抖动
- 抓手图标随方向翻转（下方弹出时斜线朝上），拖动方向始终与面板成长方向一致
- 单次拖拽/缩放的位移会被夹紧，持久化下来的偏移量始终落在「面板贴住视口边距」的范围内，不会存下 ±几千的脏值

### 面板拖动与缩放

面板一旦被拖动或缩放，就切换为「固定尺寸 + 内部滚动」模式，并出现两个额外交互：

| 操作 | 效果 |
|---|---|
| 拖动面板**头部** | 移动整块面板（抓手光标提示）；位置以「自适应位置」为基准记录位移 |
| 拖动**缩放手柄**（贴徽章那条竖边的底角） | 改尺寸，规则与 Windows 拖窗口边框一致：**抓住的那条边走，对边不动** |
| **双击头部** / 点**「复位面板」** | 清空自定义几何，回到默认尺寸并重新自动贴靠徽章 |

缩放的锚边按「面板贴哪一侧」自动选择，因此抓手**永远跟着指针走**：

| 面板开向 | 抓手位置 | 拖动行为 |
|---|---|---|
| 向右开（贴徽章左缘） | 右下角（`nwse` 光标） | 右缘跟指针：**向右拖变宽**、向左拖变窄；左缘固定 |
| 向左开（贴徽章右缘） | 左下角（`nesw` 光标） | 左缘跟指针：**向左拖变宽**、向右拖变窄；右缘固定 |
| 两种方向 | 底边 | 底边跟指针：**向下拖变高**、向上拖变矮；顶边固定 |

- 抓手常驻显示（不必先做任何操作），拖动后自动进入自定义尺寸模式；它是自绘的小按钮（斜线图标 + 边框 + `label-secondary` 着色），悬停/拖动时高亮——原生 `resize` 三角在暗色主题下几乎看不见，所以没有使用
- 抓手图标随上下弹出方向翻转（面板向下弹出时斜线朝上）
- 尺寸与位移存于 localStorage（`dsh-tidewatch-panel-v1`），刷新后保留；夹紧在可视区内（边距 8px），尺寸下限 240×180、上限 900×1200 且不超过视口；面板已贴到视口边时保持贴边而不是越界
- 未自定义时高度保持自适应（内容多高就多高，只有 `maxHeight` 上限），宽度默认 320px；「复位面板」按钮只在自定义后出现
- 从头部按钮 / 抓手按下不会触发移动拖动，避免与点击、缩放冲突

## 文件结构

```
dsh-tidewatch
├── package.json          # dsh.bundle.patch + dsh.client.platform 声明
├── cordis.patch.yml      # 装配行
├── lib/
│   ├── pricing.js        # 纯函数：峰谷窗口、isPeakHour/peakPhaseAt、双模型价格表、priceRuleFor、costOf
│   ├── index.js          # 宿主：costUsage 会话投影（按事件时刻 + 按模型分档计费）
│   └── client.js         # 前端：悬浮徽章 + 自适应方向/可拖动/可缩放面板（__ModuleLoader__ bundle）
├── test/
│   ├── verify.mjs        # 价格表/峰谷数学/前后端常量一致性自检（29 项）
│   ├── projection.mjs    # 会话投影集成自检（17 项）
│   ├── panel-box.mjs     # 弹出方向/尺寸夹紧纯函数自检（11 项）
│   └── panel.mjs         # 面板拖动/缩放跟手/夹紧/复位/翻转行为自检（19 项）
├── node_modules/zod      # 本地开发的依赖 junction（见「开发与验证」）
└── docs/PORTING.md       # 移植到其他宿主的适配说明
```

## 数据流

```
模型调用 usage 块（assistant/chunk、assistant/message 事件）
        │  lib/index.js：costUsage 会话投影（zod schema 校验）
        ▼
  token 桶 + 美元成本（按请求头模型取价格条目，按事件时刻取峰谷档位）
        │  useProjection('costUsage')（浏览器端）
        ▼
  lib/client.js：悬浮徽章渲染（秒级倒计时 + 分模型花费 + 汇率换算显示）
```

## 开发与验证

```sh
node test/verify.mjs      # 价格表 / 峰谷数学 / 模型分档 / 前后端常量一致性（29 项）
node test/projection.mjs  # 会话投影集成：分模型计费、流式替换、切档不串价（17 项）
node test/panel-box.mjs   # 弹出方向 / 尺寸夹紧纯函数（11 项，无需 DOM）
node test/panel.mjs       # 面板交互：拖动 / 缩放跟手（左右开各有覆盖）/ 夹紧 / 复位（19 项）
```

`verify.mjs` 会从 `lib/client.js` 源码里解析 `RATE_RULES` 与 `FIXED_FX`，与 `lib/pricing.js` 的
`rates.cny`、`CNY_PER_USD` 逐位比对——两处常量一旦漂移即报错，改价时不会再漏改前端。

`panel-box.mjs` 直接断言 `computePanelBox()`：它决定面板弹出方向与尺寸夹紧，是最容易回归的一环
（例如输入框被顶到视口顶端时必须翻到下方且不越界）。

`panel.mjs` 用一个最小 React 外壳渲染真实组件、直接派发 pointer 事件，因此面板的拖动/缩放/翻转行为
（含夹紧、持久化、复位）都有回归覆盖，改动交互逻辑后请一并跑它。

> **本地跑测试前先准备 `zod`**：宿主 `lib/index.js` 依赖 `zod`，而本仓库以 link 方式挂载、自身不带
> `node_modules`，直接 `node test/projection.mjs` 会报 `Cannot find package 'zod'`。任选一种：
>
> ```sh
> pnpm install                                   # 按 pnpm-lock.yaml 安装
> # 或复用 DSH 自带的一份（Windows）
> New-Item -ItemType Junction -Path node_modules\zod -Target "$env:USERPROFILE\.dsh\profiles\node_modules\zod"
> ```
>
> `test/verify.mjs` 只依赖纯函数，无需 `zod` 即可运行。

## 已知限制

- 价格表内置（官方 2026-09-10 起：`deepseek-flash` 谷时 ¥0.02 / ¥1 / ¥4，`deepseek-v4-pro` 谷时 ¥0.15 / ¥4.5 / ¥13.5，峰时均 ×2）。**官方再调价或增减在售模型后需手动同步** `lib/pricing.js`（计费）与 `lib/client.js` 的 `RATE_RULES`（展示）两处常量；`test/verify.mjs` 会校验二者一致，但不会替你发现官方改价
- 未收录的模型名一律按 Flash 价估算（宿主日志提示一次）。若 DSH 配置了非 DeepSeek 供应商（如 `llm-pi-ai` 的 opencode 路由），其模型名不在官方价格表内，金额仅为按 Flash 价的估算值
- 时段判定固定按 UTC（官方口径），时段表展示按北京时间（UTC+8）
- 花费为美元账本 × 固定汇率 6.82 换算人民币（与官方人民币标价一致）；展开面板可切换美元显示。美元单价只保留 4 位小数，缓存命中档（¥0.02/M）在美元视图下有效数字偏少，对账请用人民币视图
- 面板的自定义几何只存在本机 localStorage（`dsh-tidewatch-panel-v1`），换浏览器/清缓存后回到默认；弹出的**方向与尺寸**每次渲染都按当前徽章位置与视口重新计算，因此拖动徽章、缩放窗口、切换会话后都会自动贴合，不会残留越界位置

## 借鉴与许可

峰谷数学（isPeakHour / peakPhaseAt / tierFor / costOf）与会话投影结构借鉴自 [dsh-cost-meter](https://github.com/Han-1413141/dsh-cost-meter)（MIT License），按精简目标改写。

[MIT](LICENSE) © 2026 KhalilYamber
